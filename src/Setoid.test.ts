import {
  getArraySetoid,
  getProductSetoid,
  getRecordSetoid,
  getStructSetoid,
  getTupleSetoid,
  Setoid,
  setoidBoolean,
  setoidDate,
  setoidNumber,
  setoidString,
  strictEqual,
} from 'fp-ts/lib/Setoid'

// what setoid comparison functions ship with fp-ts?
// Using setoid.equals to memoize

describe('Setoid', () => {})
